# Optional overrides when running with docker-compose.
# Copy to .env and adjust as needed.

# POSTGRES_DB=nexin
# POSTGRES_USER=nexin
# POSTGRES_PASSWORD=nexin

# DJANGO_SECRET_KEY=your-secret-key
# DEBUG=true

# Backend URL used by Vite dev server proxy (frontend container).
# BACKEND_URL=http://backend:8000
